name: TikTok Algorithm Burner

on:
  schedule:
    - cron: '0 3 * * *'  # 3AM summoning energy
  workflow_dispatch:

jobs:
  burn:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Ignite the algorithm
        run: |
          echo "🔥 BURNING TIKTOK ALGO 🔥"
          # your brainrot script here
          python scripts/burn.py || echo "Script missing? Fix me!"
